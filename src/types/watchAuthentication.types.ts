export interface AuthenticationPlan {
  id: number;
  name: string;
  price: string;
  duration: string;
  time: string;
  description: string;
  icon: string;
  features: string[];
}

export interface BannerFeatures {
  icon: string;
  text: string;
}

